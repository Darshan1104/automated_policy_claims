# automated_policy_claims
A Streamlit RAG chatbot for insurance claims using LangGraph. Features hallucination checks, web search fallback, and fixes for infinite loops and Groq API schema errors. Includes dynamic API key input and policy ingestion.
